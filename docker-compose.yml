version: '3.9'

services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - db
  db:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: app_vestibulando
      POSTGRES_PASSWORD: eu_4m0_bern4rdin
      POSTGRES_DB: app_vestibulando